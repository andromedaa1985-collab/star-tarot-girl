# 星轨塔罗少女 App 打包准备清单

目标：后期可以把当前网页稳定包装成可下载的桌面版或手机安装包，同时不把 API key、会员权益和用户数据安全交给前端本地判断。

## 当前结论

先把线上 Web/PWA 做稳，再做壳。

当前项目已经具备 Netlify Functions 后端通道，前端只请求 `/api/*`，服务商密钥应继续放在 Netlify 环境变量或本地 `.env`，不要写进 Vite 前端环境变量、localStorage、打包配置或安装包资源里。

## 推荐路径

| 场景 | 推荐方案 | 原因 |
| --- | --- | --- |
| 现在马上可用 | Web + PWA 安装 | 改动最小，用户能从浏览器添加到桌面/主屏幕 |
| Windows/macOS 下载版 | Tauri | 包体小，适合把现有网页包成桌面应用 |
| Android 非商店分发 | Capacitor 或 PWA/TWA 包装 | 用户可下载 APK，但后端授权仍要走服务器 |
| iOS 私下分发 | 暂不作为第一目标 | 证书、签名、安装限制和维护成本都更高 |

## 已完成底座

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| Web manifest | 已完成第一刀 | `public/manifest.webmanifest`，入口为 `/app` |
| 移动端安装元信息 | 已完成第一刀 | `index.html` 已补充 theme、apple、manifest、OG 基础标签 |
| 前端包名 | 已完成第一刀 | `package.json` 改为 `astrorail-tarot-girl` |
| API key 位置 | 已完成第一刀 | 继续走服务端环境变量，不放前端包 |
| Netlify 路由 | 已有 | `/api/*` 转发到 Netlify Function |

## 打包前必须完成

1. 会员权益必须服务端判定。
   本地 App 可以被复制、破解或离线修改，不能只靠前端变量判断“已付费”。

2. 生成记录要有稳定 ID。
   塔罗、日记复盘、八字追问、人生沙盘的 AI 结果最好都有 generationId，方便追踪成本、复查失败、做用户历史。

3. 用户资料要有导出和恢复路径。
   当前如果主要依赖本地存储，换设备、清缓存或换壳后会丢上下文。上线收费前至少要有导出备份，最好有账号同步。

4. 安装包只保存公开配置。
   可以放站点 URL、版本号、主题色；不能放 DeepSeek、图片生成、支付密钥、Webhook 密钥。

5. 支付回调必须打到线上后端。
   下载版 App 内的“支付成功”页面只能展示状态，真正开通权益要以后端收到支付平台通知为准。

6. 准备更新通道。
   桌面版要有版本号、下载地址和更新说明；Android APK 至少要有版本号和手动更新提示。

7. 准备隐私与数据说明。
   塔罗/日记/命理都属于敏感个人内容，付费前需要明确说明保存位置、用途、删除方式和第三方模型调用。

## 下一步建议

第一阶段继续做 Web/PWA：把体验、错误兜底、生成记录和会员状态打稳。

第二阶段做 Tauri 桌面壳：只包装线上站点或本地静态包，所有 AI 和支付仍调用 Netlify 后端。

第三阶段再做 Android APK：先内部下载测试，再决定是否补账号体系和云端历史。
